// SPDX-License-Identifier: MIT
pragma solidity 0.8.24;

import { TestBase } from "../../TestBase.sol";

/// @notice Student-owned test scaffold. Add the exercise contract import, setup, and proof of concept.
contract ExerciseTest is TestBase {
    function setUp() public {
        // TODO: Deploy the scoped contract and prepare actors and balances.
    }

    function testDocumentedBehavior() public {
        // TODO: Add a non-exploit baseline test before writing a proof of concept.
        assertTrue(true);
    }

    function testProofOfConcept() public {
        // TODO: Reproduce the identified issue without testing unauthorized systems.
        assertTrue(true);
    }
}
